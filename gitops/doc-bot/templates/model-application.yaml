apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: {{ .Values.modelApplication.name }}
  namespace: {{ .Values.argocdNamespace }}
  annotations:
    argocd.argoproj.io/compare-options: IgnoreExtraneous
    argocd.argoproj.io/sync-wave: "2"
spec:
  project: default
  destination:
    server: 'https://kubernetes.default.svc'
    namespace: "{{ .Values.dataScienceProjectNamespace }}"
  source:
    path: "{{ .Values.modelApplication.path }}"
    repoURL: "{{ .Values.modelApplication.repoURL }}"
    targetRevision: "{{ .Values.modelApplication.targetRevision }}"
    helm:
      values: |
        argocdNamespace: {{ .Values.argocdNamespace }}
        createNamespace: false
        instanceName: {{ .Values.model.name }}
        dataScienceProjectNamespace: {{ .Values.dataScienceProjectNamespace }}
        dataScienceProjectDisplayName: {{ .Values.dataScienceProjectDisplayName }}
        model:
          root: {{ .Values.model.root }}
          id: {{ .Values.model.id }}
          name: {{ .Values.model.name }}
          displayName: {{ .Values.model.displayName }}
          enableAuth: {{ .Values.model.enableAuth }}
          rawDeployment: {{ .Values.model.rawDeployment }}
          accelerator:
            productName: {{ .Values.model.accelerator.productName }}
            min: {{ .Values.model.accelerator.min }}
            max: {{ .Values.model.accelerator.max }}
          connection:
            createSecret: {{ .Values.model.connection.createSecret }}
            name: {{ .Values.model.connection.name }}
            displayName: {{ .Values.model.connection.displayName }}
            type: {{ .Values.model.connection.type }}
            scheme: {{ .Values.model.connection.scheme }}
            awsAccessKeyId: {{ .Values.model.connection.awsAccessKeyId }}
            awsSecretAccessKey: {{ .Values.model.connection.awsSecretAccessKey }}
            awsDefaultRegion: {{ .Values.model.connection.awsDefaultRegion }}
            awsS3Bucket: {{ .Values.model.connection.awsS3Bucket }}
            awsS3Endpoint: {{ .Values.model.connection.awsS3Endpoint }}
  syncPolicy:
    automated:
      # prune: true
      selfHeal: true